/**
 * @format
 */

import { AppRegistry } from "react-native";
import { name as appName } from "./app.json";
import AwesomeApp from "./src/App";

AppRegistry.registerComponent(appName, () => AwesomeApp);
